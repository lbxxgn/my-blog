# =============================================================================
# Simple Blog 环境变量配置模板
# =============================================================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改 .env 文件中的配置值（特别是密码和密钥）
# 3. 运行启动脚本: ./start.sh
# =============================================================================

# -----------------------------------------------------------------------------
# 管理员账号配置（必需）
# -----------------------------------------------------------------------------
ADMIN_USERNAME=admin

# ⚠️ 请修改为强密码（至少12位，包含大小写字母和数字）
ADMIN_PASSWORD=YourSecurePassword123!

# -----------------------------------------------------------------------------
# 应用配置
# -----------------------------------------------------------------------------
# ⚠️ 生产环境请使用随机字符串生成密钥
# Python 生成方法: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# 调试模式（开发环境可设为 True，生产环境必须设为 False）
DEBUG=False

# 应用端口（默认 5001）
PORT=5001

# -----------------------------------------------------------------------------
# 网站信息配置
# -----------------------------------------------------------------------------
SITE_NAME=我的博客
SITE_DESCRIPTION=一个简单的博客系统
SITE_AUTHOR=管理员

# -----------------------------------------------------------------------------
# HTTPS 配置（可选）
# -----------------------------------------------------------------------------
# 如果配置了 HTTPS 证书，设置为 True
FORCE_HTTPS=False

# Flask 运行环境
FLASK_ENV=production

# -----------------------------------------------------------------------------
# AI功能配置（可选）
# -----------------------------------------------------------------------------
# AI提供商：openai, volcengine, dashscope
# AI_PROVIDER=openai

# OpenAI API密钥（如果使用OpenAI）
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 火山引擎API密钥（如果使用火山引擎/豆包）
# 获取方式：https://console.volcengine.com/ark
# VOLCENGINE_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 阿里百炼API密钥（如果使用阿里百炼/通义千问）
# 获取方式：https://dashscope.console.aliyun.com/
# DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# AI模型（可选，留空使用默认模型）
# OpenAI默认: gpt-3.5-turbo
# 火山引擎默认: doubao-pro-4k
# 阿里百炼默认: qwen-turbo
# AI_MODEL=

# 启用AI标签生成（默认关闭，设置为1或true启用）
# AI_TAG_GENERATION_ENABLED=0
