(function(){'use strict';let currentPage='home';let currentMyPostsTab='published';document.addEventListener('DOMContentLoaded',function(){initBottomNavigation();initDoubleTapToTop();initMobileLayout();initMyPostsTabs();initProfileAdminLink();});function initBottomNavigation(){const navItems=document.querySelectorAll('.nav-item[data-page]');navItems.forEach(item=>{item.addEventListener('click',function(e){const page=this.getAttribute('data-page');if(page==='publish'){e.preventDefault();if(typeof window.openMobileEditor==='function'){window.openMobileEditor();}
return;}
if(page==='home'&&window.location.pathname!=='/'){return;}
e.preventDefault();if(page!==currentPage){setActiveNavItem(page);switchPage(page);}});});}
function setActiveNavItem(page){document.querySelectorAll('.nav-item[data-page]').forEach(item=>{item.classList.remove('active');if(item.getAttribute('data-page')===page){item.classList.add('active');}});currentPage=page;}
function switchPage(page){if(window.innerWidth>768){return;}
document.querySelectorAll('.mobile-page').forEach(p=>{p.style.display='none';});const targetPage=document.querySelector(`.mobile-page[data-page="${page}"]`);if(targetPage){targetPage.style.display='block';}
if(page==='my-posts'){loadMyPosts(currentMyPostsTab);}}
function initDoubleTapToTop(){let lastTap=0;const homeNav=document.querySelector('.nav-item[data-page="home"]');if(homeNav){homeNav.addEventListener('click',function(e){const now=Date.now();if(now-lastTap<300){e.preventDefault();scrollToTop();}
lastTap=now;});}}
function scrollToTop(){window.scrollTo({top:0,behavior:'smooth'});}
function initMobileLayout(){if(window.innerWidth<=768){enableMobileLayout();}
window.addEventListener('resize',function(){if(window.innerWidth<=768){enableMobileLayout();}else{disableMobileLayout();}});window.addEventListener('hashchange',function(){if(window.location.hash&&window.location.hash!=='#home'){history.replaceState(null,'',' ');}});if(window.innerWidth<=768){setActiveNavItem('home');if(window.location.hash&&window.location.hash!=='#home'){history.replaceState(null,'',' ');}}else{if(window.location.hash){history.replaceState(null,'',' ');}}}
function initMyPostsTabs(){const tabButtons=document.querySelectorAll('.mobile-posts-tab');if(tabButtons.length===0){return;}
tabButtons.forEach(button=>{button.addEventListener('click',function(){const tab=this.getAttribute('data-tab')||'published';currentMyPostsTab=tab;tabButtons.forEach(btn=>btn.classList.remove('active'));this.classList.add('active');loadMyPosts(tab);});});}
async function loadMyPosts(tab){const container=document.getElementById('my-posts-list');if(!container){return;}
container.innerHTML='<p style="text-align: center; color: var(--text-secondary); padding: 40px 0;">加载中...</p>';try{const response=await fetch(`/mobile/my-posts?tab=${encodeURIComponent(tab)}`);const data=await response.json();if(response.status===401){container.innerHTML='<p style="text-align: center; color: var(--text-secondary); padding: 40px 0;"><a href="/login" style="color: var(--primary-color);">登录后查看 →</a></p>';return;}
if(!data.posts||data.posts.length===0){container.innerHTML=`<p style="text-align: center; color: var(--text-secondary); padding: 40px 0;">${tab==='drafts'?'还没有草稿':'还没有已发布文章'}</p>`;return;}
container.className='posts-list';container.innerHTML=data.posts.map(renderMyPostItem).join('');}catch(error){console.error('Failed to load my posts:',error);container.innerHTML='<p style="text-align: center; color: var(--text-secondary); padding: 40px 0;">加载失败，请稍后重试</p>';}}
function renderMyPostItem(post){const title=escapeHtml(post.title||'无标题');const category=post.category_name?`<span class="post-category">${escapeHtml(post.category_name)}</span>`:'';const status=post.is_published?'已发布':'草稿';const access=formatAccess(post.access_level);const editHref=`/admin/edit/${post.id}`;const href=post.is_published?`/post/${post.id}`:editHref;return`<article class="post-card"style="position: relative;"><!--操作按钮组--><div class="my-post-actions"><button class="my-post-delete-btn"onclick="confirmDeletePost(${post.id}, '${escapeHtml(post.title || '无标题')}')"aria-label="删除文章">🗑</button><a href="${editHref}"class="my-post-edit-btn"aria-label="编辑文章">✎</a></div><a href="${href}"class="post-card-link"><div class="post-card-image"><span class="post-card-image-placeholder">${post.is_published?'📝':'📄'}</span></div><div class="post-card-content"><h2>${title}</h2><div class="post-excerpt">${escapeHtml(truncateContent(post.content||'',100))}</div><div class="post-meta"><span>${status}</span>${category}<span>${access}</span><time>${formatDate(post.created_at)}</time></div></div></a></article>`;}
function truncateContent(content,maxLength){const text=String(content||'').replace(/<[^>]+>/g,'');if(text.length<=maxLength){return text;}
return`${text.slice(0,maxLength)}...`;}
function escapeHtml(text){const div=document.createElement('div');div.textContent=text||'';return div.innerHTML;}
function formatDate(dateStr){if(!dateStr){return'';}
const date=new Date(dateStr);if(Number.isNaN(date.getTime())){return String(dateStr).slice(0,10);}
return date.toISOString().slice(0,10);}
function formatAccess(accessLevel){const accessMap={public:'公开',login:'登录可见',password:'密码保护',private:'私密'};return accessMap[accessLevel]||'公开';}
function enableMobileLayout(){document.body.classList.add('mobile-layout-active');}
function disableMobileLayout(){document.body.classList.remove('mobile-layout-active');}
function confirmDeletePost(postId,postTitle){const modal=document.createElement('div');modal.className='delete-confirm-modal';modal.innerHTML=`<div class="delete-confirm-content"><div class="delete-confirm-icon">🗑</div><h3>确认删除文章</h3><p>确定要删除文章《${postTitle}》吗？</p><p class="delete-confirm-warning">此操作无法撤销</p><div class="delete-confirm-actions"><button class="delete-confirm-btn delete-confirm-btn-cancel"onclick="closeDeleteModal()">取消</button><button class="delete-confirm-btn delete-confirm-btn-confirm"onclick="deletePost(${postId})">确认删除</button></div></div>`;modal.addEventListener('click',function(e){if(e.target===modal){closeDeleteModal();}});document.body.appendChild(modal);setTimeout(()=>{modal.classList.add('show');},10);window.deleteModal=modal;}
function closeDeleteModal(){if(window.deleteModal){window.deleteModal.classList.remove('show');setTimeout(()=>{window.deleteModal.remove();window.deleteModal=null;},300);}}
async function deletePost(postId){try{const csrfToken=document.querySelector('meta[name="csrf_token"]')?.getAttribute('content')||'';const response=await fetch(`/admin/delete/${postId}`,{method:'POST',headers:{'Content-Type':'application/json','X-CSRFToken':csrfToken}});const data=await response.json();if(data.success){closeDeleteModal();showToast('文章已删除');loadMyPosts(currentMyPostsTab);}else{showToast('删除失败：'+(data.error||'未知错误'));}}catch(error){console.error('Failed to delete post:',error);showToast('删除失败，请稍后重试');}}
function showToast(message){const toast=document.createElement('div');toast.className='toast-message';toast.textContent=message;document.body.appendChild(toast);setTimeout(()=>{toast.classList.add('show');},10);setTimeout(()=>{toast.classList.remove('show');setTimeout(()=>{toast.remove();},300);},2000);}
function initProfileAdminLink(){const link=document.querySelector('.profile-link-admin');if(!link){return;}link.addEventListener('click',function(e){if(window.innerWidth<=768){e.preventDefault();setActiveNavItem('my-posts');switchPage('my-posts');}});}
window.MobileLayout={setActiveNavItem,switchPage,scrollToTop,loadMyPosts};window.confirmDeletePost=confirmDeletePost;window.closeDeleteModal=closeDeleteModal;window.deletePost=deletePost;})();