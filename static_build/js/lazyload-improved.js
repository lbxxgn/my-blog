class LazyLoadImage{constructor(){this.init();}
init(){this.supportsIntersectionObserver='IntersectionObserver'in window;this.supportsWebP=this.checkWebPSupport();this.imageCache=new Map();this.resizeTimer=null;this.options={rootMargin:'100px 0px',threshold:0.01,animationDuration:300,maxRetries:2};if(this.supportsIntersectionObserver){this.observer=new IntersectionObserver(this.onIntersection.bind(this),this.options);}
this.initImages();this.setupResponsiveImages();window.addEventListener('resize',this.onResize.bind(this));}
checkWebPSupport(){try{const canvas=document.createElement('canvas');if(canvas.getContext&&canvas.getContext('2d')){return canvas.toDataURL('image/webp').indexOf('data:image/webp')===0;}}catch(e){console.warn('WebP support check failed:',e);}
return false;}
initImages(){const images=document.querySelectorAll('img[data-src]:not(.lazy-processed)');images.forEach(img=>this.setupImage(img));if(this.supportsIntersectionObserver){images.forEach(img=>this.observer.observe(img));}else{images.forEach(img=>this.loadImage(img));}}
setupImage(img){if(img.classList.contains('lazy-processed'))return;const src=img.getAttribute('data-src');const placeholder=img.getAttribute('data-placeholder');const alt=img.getAttribute('alt')||'';if(placeholder&&!img.src){img.src=placeholder;}else{img.src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1 1'%3E%3Crect width='1' height='1' fill='%23f3f4f6'/%3E%3C/svg%3E";}
img.classList.add('lazy-loading','lazy-processed');img.setAttribute('alt',alt);img.dataset.originalAlt=alt;}
onIntersection(entries){entries.forEach(entry=>{if(entry.isIntersecting){const img=entry.target;this.loadImage(img);this.observer.unobserve(img);}});}
loadImage(img,retryCount=0){if(img.classList.contains('lazy-loaded'))return;const src=img.getAttribute('data-src');const srcset=img.getAttribute('data-srcset');const sizes=img.getAttribute('data-sizes');if(!src){console.warn('No data-src attribute found for image',img);return;}
img.classList.remove('lazy-loading');img.classList.add('lazy-load-in-progress');if(sizes&&srcset){this.makeResponsive(img,sizes);}
const tempImg=new Image();tempImg.onload=()=>{img.style.opacity='0';img.style.transition=`opacity ${this.options.animationDuration}ms ease-in-out`;img.src=src;if(srcset){img.srcset=srcset;if(sizes){img.sizes=sizes;}}
img.classList.remove('lazy-load-in-progress');img.classList.add('lazy-loaded');requestAnimationFrame(()=>{img.style.opacity='1';});img.dispatchEvent(new CustomEvent('lazyload:loaded',{detail:{src:src}}));};tempImg.onerror=()=>{if(retryCount<this.options.maxRetries){setTimeout(()=>{this.loadImage(img,retryCount+1);},1000*(retryCount+1));}else{img.classList.remove('lazy-load-in-progress');img.classList.add('lazy-load-error');img.src=this.getErrorPlaceholder();console.error(`Failed to load image after ${retryCount+1}attempts:${src}`);img.dispatchEvent(new CustomEvent('lazyload:error',{detail:{src:src,retries:retryCount+1}}));}};if(img.crossOrigin){tempImg.crossOrigin=img.crossOrigin;}
tempImg.src=src;this.imageCache.set(src,tempImg);}
makeResponsive(img,sizesJson){try{const sizesMap=JSON.parse(sizesJson);const width=this.getElementWidth(img);let selectedSize='original';if(width<600){selectedSize='thumbnail';}else if(width<1200){selectedSize='medium';}else if(width<1920){selectedSize='large';}
if(this.supportsWebP&&sizesMap[`${selectedSize}_webp`]){img.setAttribute('data-src',sizesMap[`${selectedSize}_webp`]);}else if(sizesMap[selectedSize]){img.setAttribute('data-src',sizesMap[selectedSize]);}}catch(e){console.error('Error parsing data-sizes:',e);}}
getElementWidth(element){if(!element)return window.innerWidth;if(!document.contains(element)){return window.innerWidth;}
const rect=element.getBoundingClientRect();return rect.width||element.offsetWidth||element.clientWidth||window.innerWidth;}
onResize(){clearTimeout(this.resizeTimer);this.resizeTimer=setTimeout(()=>{this.setupResponsiveImages();},300);}
setupResponsiveImages(){const images=document.querySelectorAll('img[data-sizes]:not(.lazy-responsive)');images.forEach(img=>{img.classList.add('lazy-responsive');this.makeResponsive(img,img.getAttribute('data-sizes'));});}
getErrorPlaceholder(){return"data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 400 300'%3E%3Crect width='400' height='300' fill='%23f3f4f6'/%3E%3Ctext x='50%25' y='50%25' text-anchor='middle' dominant-baseline='middle' fill='%239ca3af' font-family='sans-serif' font-size='16'%3E图片加载失败%3C/text%3E%3C/svg%3E";}
observeNewImages(selector){let images=[];if(typeof selector==='string'){images=document.querySelectorAll(`${selector}:not(.lazy-processed)`);}else if(selector instanceof HTMLElement){images=[selector];}else if(selector&&typeof selector.forEach==='function'){images=selector;}
images.forEach(img=>this.setupImage(img));if(this.supportsIntersectionObserver){images.forEach(img=>this.observer.observe(img));}else{images.forEach(img=>this.loadImage(img));}}
destroy(){if(this.supportsIntersectionObserver){this.observer.disconnect();}
window.removeEventListener('resize',this.onResize.bind(this));this.imageCache.clear();}}
const lazyLoadImage=new LazyLoadImage();document.addEventListener('DOMContentLoaded',()=>{lazyLoadImage.initImages();lazyLoadImage.setupResponsiveImages();});window.lazyLoadImage=lazyLoadImage;window.setupContentImages=(selector='.post-content img')=>{lazyLoadImage.observeNewImages(selector);};window.observeNewImages=(selector='img[data-src]')=>{lazyLoadImage.observeNewImages(selector);};if(typeof MutationObserver!=='undefined'){const observer=new MutationObserver((mutations)=>{mutations.forEach((mutation)=>{if(mutation.addedNodes.length){mutation.addedNodes.forEach((node)=>{if(node.nodeType===Node.ELEMENT_NODE){const images=node.querySelectorAll?node.querySelectorAll('img[data-src]:not(.lazy-processed)'):[];images.forEach(img=>lazyLoadImage.setupImage(img));if(lazyLoadImage.supportsIntersectionObserver){images.forEach(img=>lazyLoadImage.observer.observe(img));}else{images.forEach(img=>lazyLoadImage.loadImage(img));}}});}});});observer.observe(document.body,{childList:true,subtree:true});}